<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ldh.exam.demo.repository.BoardRepository">

	<select id="getBoardByBoardId" resultType="Board">
		SELECT B.*
		FROM board
		AS B
		WHERE B.id = #{ id }
	</select>

	<select id="getBoards" resultType="Board">
		SELECT B.*
		FROM board AS B
	</select>

	<select id="getCategoryByBoardIdAndRelId" resultType="Category">
		SELECT C.*
		FROM
		category AS C
		WHERE C.boardId = #{boardId}
		AND C.relId = #{relId}
	</select>

	<select id="getCategories" resultType="Category">
		SELECT C.*
		FROM
		category
		AS C
	</select>

	<insert id="makeGuideForWriteRecipe">
		INSERT INTO
		guide
		SET regDate=NOW(),
		updateDate=NOW(),
		sortId=#{sortId},
		methodId=#{methodId},
		contentId=#{contentId},
		freeId=#{freeId}
	</insert>

	<select id="getLastInsertId" resultType="int">
		SELECT
		LAST_INSERT_ID()
	</select>

	<update id="updateRecipeId">
		UPDATE guide
		SET recipeId = #{recipeId}
		WHERE id =
		#{guideId}
	</update>

	<update id="updateGuide">
		UPDATE guide
		<set>
			<if test="sortId != null and sortId !=''">
				sortId = #{sortId},
			</if>
			<if test="methodId != null and methodId !=''">
				`methodId` = #{methodId},
			</if>
			<if test="contentId != null and contentId !=''">
				`contentId` = #{contentId},
			</if>
			<if test="freeId != null and freeId !=''">
				`freeId` = #{freeId},
			</if>
			updateDate = NOW()
		</set>
		WHERE id = #{id}
	</update>

	<select id="getCategoriesAboutRecipe" resultType="Category">
		SELECT C.*
		FROM
		guide AS G
		INNER JOIN category AS C
		WHERE G.recipeId=#{recipeId}
		AND (
		C.boardId=1
		AND G.sortId=C.relId
		OR
		C.boardId=2
		AND G.methodId=C.relId
		OR
		C.boardId=3
		AND G.contentId=C.relId
		OR
		C.boardId=4 AND G.freeId=C.relId)
	</select>

</mapper> 