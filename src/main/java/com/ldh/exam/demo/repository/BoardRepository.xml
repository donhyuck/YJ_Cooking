<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ldh.exam.demo.repository.BoardRepository">

	<select id="getBoardByBoardId" resultType="Board">
		SELECT B.*
		FROM board
		AS B
		WHERE B.id = #{ id }
	</select>

	<select id="getBoards" resultType="Board">
		SELECT B.*
		FROM board AS B
	</select>

	<select id="getBoardNameByBoardId" resultType="String">
		SELECT boardName
		FROM
		category AS C
		WHERE boardId =
		#{boardId}
	</select>

	<select id="getCategoryByBoardId" resultType="Category">
		SELECT C.*
		FROM
		category AS C
		WHERE boardId = #{boardId}
	</select>

	<select id="getCategories" resultType="Category">
		SELECT C.*
		FROM
		category
		AS C
	</select>

	<select id="getGuideIdByBoardIdAndRelId" resultType="int">
		SELECT G.id
		FROM guide AS G
		WHERE 1
		<choose>
			<when test="boardId== 1">
				AND G.sortId = #{relId}
			</when>
			<when test="boardId== 2">
				AND G.sortId = #{relId}
			</when>
			<when test="boardId== 3">
				AND G.sortId = #{relId}
			</when>
			<when test="boardId== 4">
				AND G.sortId = #{relId}
			</when>
			<otherwise>
				AND 1
			</otherwise>
		</choose>
	</select>

</mapper> 